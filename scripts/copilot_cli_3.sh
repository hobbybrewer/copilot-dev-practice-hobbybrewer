#!/bin/bash
shopt -s expand_aliases
eval "$(github-copilot-cli alias -- "$0")"

echo "?? Get request to http://localhost:8080/api/model with query string search = url encoded UWS Brownstone Near Central Park and store the beautified response in file response.json"
?? Get request to http://localhost:8080/api/model with query string search = url encoded UWS Brownstone Near Central Park and store the beautified response in file response.json

echo "?? In response.json print the key id inside listingsAndReviews array"
?? In response.json print the key id inside listingsAndReviews array